# Example environment variables for Stars of the Past
# NASA APOD public demo key (replace with your own for production or use this value. The DEMO_KEY is provided by NASA for public use and allows a limited number of requests per hour/day.)
NASA_API_KEY=DEMO_KEY

# Backend API URL (for local development)
VITE_API_URL=http://localhost:3000
